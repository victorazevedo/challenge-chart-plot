const secondstamp = 1000;
const minutestamp = 60 * secondstamp;
const hourstamp = 60 * minutestamp;
const daystamp = 24 * hourstamp;

//Timestampos constants
const TimestampConstans = {
    secondstamp: secondstamp,
    minutestamp: minutestamp,
    hourstamp: hourstamp,
    daystamp: daystamp
}
export default TimestampConstans;